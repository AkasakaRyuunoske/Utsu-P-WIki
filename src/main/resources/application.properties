server.port = 8080

jwt.secret = sanmandefuanteinakonosekaikakumeihookoshiteyo

spring.jpa.hibernate.ddl-auto       = update
spring.datasource.url               = ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/utsu_p_wiki}
spring.datasource.username          = ${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password          = ${SPRING_DATASOURCE_PASSWORD:}
spring.datasource.driver-class-name = com.mysql.jdbc.Driver
spring.jpa.show-sql                 = true

spring.session.store-type = jdbc
spring.session.jdbc.initiate-schema = always
spring.session.timeout.seconds=900

server.error.whitelabel.enabled=false
spring.mvc.throw-exception-if-no-handler-found=true
spring.resources.add-mappings=false
#debug = true
#logging.level.org.springframework=DEBUG

logging.file.name = logs.log

#Headers: Authorization  Oleg {yo token}