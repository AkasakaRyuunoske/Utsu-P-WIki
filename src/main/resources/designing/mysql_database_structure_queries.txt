#create database and then try to run the application, 
CREATE DATABASE utsu_p_wiki;

#hibernate in most cases will automaticly create database structure 
#at first run(if application.properties is update).

#Here will queries to create  tables if not created automaticly by hibernate.


#Here will be data to add. This data is expected by some pages and for now must be added manually